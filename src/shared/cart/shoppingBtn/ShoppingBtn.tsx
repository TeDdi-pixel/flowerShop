import { Link } from "react-router-dom";

const ShoppingBtn = () => {
  return (
    <Link to="/" className="cart__shopping-btn">
      Continue shopping
    </Link>
  );
};

export default ShoppingBtn;
